import { by, element } from 'detox'

export const chooseLanguageButton = () => element(by.id('chooseLangButton'))
export const acceptTosCheckbox = () => element(by.id('acceptTosCheckbox'))
export const acceptTosButton = () => element(by.id('acceptTosButton'))