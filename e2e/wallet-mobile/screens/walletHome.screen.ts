import {by, element} from 'detox'

export const sendButton = () => element(by.id('sendButton'))
export const receiveButton = () => element(by.id('receiveButton'))
export const swapButton = () => element(by.id('swapButton'))
export const buyButton = () => element(by.id('buyButton'))
