/**
 * Hook to track domain usage and update lastUsed timestamp
 * This hook is now deprecated - favorites are saved when user clicks Next button
 * after successful resolution. Keeping this file for potential future use.
 */
export const useTrackDomainUsage = () => {
  // No-op: favorites are now saved in handleOnNext when user clicks Next
}
