# The Appfile is a specification of your app and its metadata
# Learn more: https://docs.fastlane.tools/advanced/#appfile

# App identifiers for both apps
app_identifier([
  "com.emurgo.yoroi",           # Production iOS
  "com.emurgo.yoroi-nightly"    # Nightly iOS
])

# Android package names
package_name([
  "com.emurgo",                 # Production Android
  "com.emurgo.nightly"          # Nightly Android
])

# Apple ID for App Store Connect
apple_id("tool-apple@anzens.com")

# Team ID for Apple Developer account
itc_team_id("119491519") # App Store Connect Team ID
team_id("F8NVT2G2L4") # Developer Portal Team ID

# It's recommended to store your app's identifiers in environment variables
# app_identifier(ENV["APP_IDENTIFIER"])
# apple_id(ENV["APPLE_ID"])
# team_id(ENV["TEAM_ID"])
