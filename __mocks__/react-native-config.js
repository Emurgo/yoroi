// @flow

/**
 * note: some tests require mainnet configuration (e.g. backend api tests),
 * so setting this flag to true will result in some tests failing
 */
export default {
  USE_TESTNET: 'false',
}
