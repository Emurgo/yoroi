/**
 * @format
 */

import {AppRegistry} from 'react-native'
import StorybookScreen from './src/StorybookScreen'

import {name as appName} from './app.json'

AppRegistry.registerComponent(appName, () => StorybookScreen)
