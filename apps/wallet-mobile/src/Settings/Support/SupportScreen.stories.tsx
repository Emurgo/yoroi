import {storiesOf} from '@storybook/react-native'
import React from 'react'

import {SupportScreen} from './SupportScreen'

storiesOf('SupportScreen', module).add('Default', () => {
  return <SupportScreen />
})
