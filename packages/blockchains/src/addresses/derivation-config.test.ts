import {derivationConfig} from './derivation-config'

describe('derivationConfig', () => {
  // NOTE: while not in use within the package for coverage
  it('should be defined', () => {
    expect(derivationConfig).toBeDefined()
  })
})
