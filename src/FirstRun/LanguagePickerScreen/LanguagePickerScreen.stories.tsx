import {storiesOf} from '@storybook/react-native'
import React from 'react'

import {LanguagePickerScreen} from '.'

storiesOf('LanguagePickerScreen', module).add('Default', () => <LanguagePickerScreen />)
