// @flow
import {COLORS} from '../../../styles/config'

const styles = {
  container: {
    flex: 1,
    marginBottom: 15,
  },
  dayContainer: {
    flex: 0,
    flexShrink: 1,
    marginTop: 10,
    marginBottom: 15,
  },
  dateLabelContainer: {
    borderBottomWidth: 1,
    borderBottomColor: COLORS.LIGHT_GRAY,
    paddingBottom: 15,
  },
  dateLabel: {
    color: COLORS.BLACK,
    fontSize: 20,
  },
}

export default styles
