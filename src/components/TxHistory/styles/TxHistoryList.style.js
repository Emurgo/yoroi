// @flow
import {COLORS} from '../../../styles/config'

const styles = {
  container: {
    flex: 1,
    marginBottom: 15,
  },
  dateLabelContainer: {
    borderBottomWidth: 1,
    borderBottomColor: COLORS.LIGHT_GRAY,
    paddingTop: 15,
    paddingBottom: 15,
    flex: 1,
    alignItems: 'center',
  },
  dateLabel: {
    color: COLORS.BLACK,
    fontSize: 20,
  },
}

export default styles
