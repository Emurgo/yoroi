import {storiesOf} from '@storybook/react-native'
import React from 'react'

import EmptyHistory from './EmptyHistory'

storiesOf('V2/Components/TxHistory/EmptyHistory', module).add('default', () => <EmptyHistory />)
