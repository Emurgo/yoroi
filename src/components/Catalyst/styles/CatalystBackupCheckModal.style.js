// @flow
import {theme} from '../../../styles/config'

const style = {
  title: {
    textAlign: 'center',
    color: '#163fa0',
    fontSize: 20,
    lineHeight: 22,
    fontWeight: 'bold',
    marginBottom: theme.spacing.paragraphBottomMargin,
  },
  checkbox: {
    marginBottom: theme.spacing.paragraphBottomMargin,
  },
}

export default style
