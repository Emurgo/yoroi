// @flow
import {COLORS} from '../../../styles/config'

const styles = {
  container: {
    flex: 0,
    backgroundColor: COLORS.BLACK,
    padding: 5,
  },
  qrContent: {
    color: COLORS.WHITE,
    textAlign: 'center',
  },
}

export default styles
